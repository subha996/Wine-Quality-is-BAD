from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="mlops package wine quality is bad",
    author="osprey",
    packages=find_packages(),
    license="MIT",
)
